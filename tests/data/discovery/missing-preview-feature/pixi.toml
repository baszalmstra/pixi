[workspace]
name = "test"
channels = ["conda-forge"]
platforms = ["linux-64"]

[package]
name = "test-package"
version = "0.1.0"

[package.build]
backend = { name = "pixi-build-python", version = "*" }
